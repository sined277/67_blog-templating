class Post:
    def __init__(self, post_id, title, subtitle, body):
        # Constructor to initialize the Post object with given attributes
        self.id = post_id       # Assigning the post_id to the 'id' attribute of the Post object
        self.title = title      # Assigning the title to the 'title' attribute of the Post object
        self.subtitle = subtitle    # Assigning the subtitle to the 'subtitle' attribute of the Post object
        self.body = body        # Assigning the body to the 'body' attribute of the Post object
